<h1>Listing wods</h1>

<table>
  <tr>
    <th>Date</th>
    <th>Time of day</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wods.each do |wod| %>
  <tr>
    <td><%= wod.date %></td>
    <td><%= wod.time_of_day %></td>
    <td><%= wod.description %></td>
    <td><%= link_to 'Show', wod %></td>
    <td><%= link_to 'Edit', edit_wod_path(wod) %></td>
    <td><%= link_to 'Destroy', wod, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wod', new_wod_path %>
